$('.users').html("<%=j render @users %>");
$('.pagination').replaceWith("<%=j paginate @users, remote: true %>");
$('.tabs .tab-link').removeClass('active');
$('.tabs [data-tab="everyone"]').addClass('active');
