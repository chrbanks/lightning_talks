$('.users').html("<%=j render @users %>");
$('.pagination').replaceWith("<%=j paginate @users, params: { controller: 'users', action: 'speakers' }, remote: true %>");
$('.tabs .tab-link').removeClass('active');
$('.tabs [data-tab="speakers"]').addClass('active');
