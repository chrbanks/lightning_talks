<%= link_to category_path(category.name), class: 'small strong quiet category' do %>
  <%= content_tag :span, nil, class: "#{category.color} img-circle pad0 inline" %><%= category.name.humanize %>
<% end %>
